        {/* CONFIGURAÇÃO DO RELATÓRIO WHITE-LABEL */}
        <Card className="mb-6 border-2 border-indigo-200 shadow-md">
          <CardHeader className="bg-indigo-50 border-b border-indigo-100">
            <CardTitle className="text-xl flex items-center text-indigo-900">
              <FileText className="h-6 w-6 mr-2 text-indigo-600" />
              Setup do Relatório Consultivo (White-Label)
            </CardTitle>
          </CardHeader>
          <CardContent className="pt-6">
            <p className="text-sm text-slate-600 mb-6">
              Configure as informações que aparecerão no PDF de planejamento tributário que será entregue ao cliente <strong>{client.companyName}</strong>.
            </p>
            
            <form action={`/api/pdf/${client.id}`} method="GET" className="grid md:grid-cols-2 gap-6">
              <div className="space-y-4">
                <div>
                  <label htmlFor="consultor" className="block text-sm font-medium text-slate-700 mb-1">Nome do Consultor / Assinatura</label>
                  <input type="text" id="consultor" name="consultor" placeholder="Seu nome" defaultValue={user?.email || ''} className="w-full rounded-md border border-slate-300 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500" />
                </div>
                <div>
                  <label htmlFor="cenarioBase" className="block text-sm font-medium text-slate-700 mb-1">Cenário a Destacar</label>
                  <select id="cenarioBase" name="cenarioBase" className="w-full rounded-md border border-slate-300 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500">
                    <option value="ideal">Recomendação Ideal ({simulacoes.length > 0 ? simulacoes[0].regime.replace('_', ' ') : 'N/A'})</option>
                    <option value="simples">Forçar Comparação: Simples Nacional</option>
                    <option value="presumido">Forçar Comparação: Lucro Presumido</option>
                  </select>
                </div>
              </div>
              
              <div className="space-y-4">
                <div>
                  <label htmlFor="mensagem" className="block text-sm font-medium text-slate-700 mb-1">Mensagem de Introdução (Opcional)</label>
                  <textarea id="mensagem" name="mensagem" rows={3} placeholder="Escreva uma breve análise executiva para o cliente..." className="w-full rounded-md border border-slate-300 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500"></textarea>
                </div>
              </div>
              
              <div className="md:col-span-2 flex justify-end gap-4 mt-4 pt-4 border-t">
                <Button type="button" variant="outline" className="text-slate-600">
                  <PieChart className="h-4 w-4 mr-2" />
                  Preview Rápido
                </Button>
                <Button type="submit" size="lg" className="bg-indigo-600 hover:bg-indigo-700 shadow-md transition-transform hover:-translate-y-1">
                  <FileText className="h-5 w-5 mr-2" />
                  Gerar Relatório PDF em Alta
                </Button>
              </div>
            </form>
          </CardContent>
        </Card>
